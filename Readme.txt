Add the required libraries

change below properties in main.py file:
app.config['MYSQL_DATABASE_USER'] = 'root'
app.config['MYSQL_DATABASE_PASSWORD'] = 'admin#123'
app.config['MYSQL_DATABASE_DB'] = 'ecoomerce_ai'
app.config['MYSQL_DATABASE_HOST'] = 'localhost'

run the script mySql.sql into database.

run the project.

open below url into the browser:
http://127.0.0.1:5000/

create a new user by clicking on signup page.
After signup, login with latest created user name and password



